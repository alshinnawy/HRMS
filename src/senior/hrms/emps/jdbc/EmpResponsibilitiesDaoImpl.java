/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import java.util.Date;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpResponsibilitiesDaoImpl extends AbstractDAO implements EmpResponsibilitiesDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_RESP_ID, EMPLOYEE_ID, RESPONSIBILITY_ID, RECEIVED_DATE, DELIVERED_DATE, NOTE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_RESP_ID, EMPLOYEE_ID, RESPONSIBILITY_ID, RECEIVED_DATE, DELIVERED_DATE, NOTE ) VALUES ( ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_RESP_ID = ?, EMPLOYEE_ID = ?, RESPONSIBILITY_ID = ?, RECEIVED_DATE = ?, DELIVERED_DATE = ?, NOTE = ? WHERE EMP_RESP_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_RESP_ID = ?";

	/** 
	 * Index of column EMP_RESP_ID
	 */
	protected static final int COLUMN_EMP_RESP_ID = 1;

	/** 
	 * Index of column EMPLOYEE_ID
	 */
	protected static final int COLUMN_EMPLOYEE_ID = 2;

	/** 
	 * Index of column RESPONSIBILITY_ID
	 */
	protected static final int COLUMN_RESPONSIBILITY_ID = 3;

	/** 
	 * Index of column RECEIVED_DATE
	 */
	protected static final int COLUMN_RECEIVED_DATE = 4;

	/** 
	 * Index of column DELIVERED_DATE
	 */
	protected static final int COLUMN_DELIVERED_DATE = 5;

	/** 
	 * Index of column NOTE
	 */
	protected static final int COLUMN_NOTE = 6;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 6;

	/** 
	 * Index of primary-key column EMP_RESP_ID
	 */
	protected static final int PK_COLUMN_EMP_RESP_ID = 1;

	/** 
	 * Inserts a new row in the EMP_RESPONSIBILITIES table.
	 */
	public EmpResponsibilitiesPk insert(EmpResponsibilities dto) throws EmpResponsibilitiesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getEmpRespId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getResponsibilityId() );
			stmt.setTimestamp(index++, dto.getReceivedDate()==null ? null : new java.sql.Timestamp( dto.getReceivedDate().getTime() ) );
			stmt.setTimestamp(index++, dto.getDeliveredDate()==null ? null : new java.sql.Timestamp( dto.getDeliveredDate().getTime() ) );
			stmt.setString( index++, dto.getNote() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpResponsibilitiesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_RESPONSIBILITIES table.
	 */
	public void update(EmpResponsibilitiesPk pk, EmpResponsibilities dto) throws EmpResponsibilitiesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getEmpRespId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getResponsibilityId() );
			stmt.setTimestamp(index++, dto.getReceivedDate()==null ? null : new java.sql.Timestamp( dto.getReceivedDate().getTime() ) );
			stmt.setTimestamp(index++, dto.getDeliveredDate()==null ? null : new java.sql.Timestamp( dto.getDeliveredDate().getTime() ) );
			stmt.setString( index++, dto.getNote() );
			stmt.setInt( 7, pk.getEmpRespId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpResponsibilitiesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_RESPONSIBILITIES table.
	 */
	public void delete(EmpResponsibilitiesPk pk) throws EmpResponsibilitiesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getEmpRespId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpResponsibilitiesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_RESPONSIBILITIES table that matches the specified primary-key value.
	 */
	public EmpResponsibilities findByPrimaryKey(EmpResponsibilitiesPk pk) throws EmpResponsibilitiesDaoException
	{
		return findByPrimaryKey( pk.getEmpRespId() );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'EMP_RESP_ID = :empRespId'.
	 */
	public EmpResponsibilities findByPrimaryKey(int empRespId) throws EmpResponsibilitiesDaoException
	{
		EmpResponsibilities ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_RESP_ID = ?", new Object[] {  new Integer(empRespId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria ''.
	 */
	public EmpResponsibilities[] findAll() throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_RESP_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpResponsibilities[] findByEmployees(int employeeId) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ?", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'RESPONSIBILITY_ID = :responsibilityId'.
	 */
	public EmpResponsibilities[] findByResponsibilities(int responsibilityId) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE RESPONSIBILITY_ID = ?", new Object[] {  new Integer(responsibilityId) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'EMP_RESP_ID = :empRespId'.
	 */
	public EmpResponsibilities[] findWhereEmpRespIdEquals(int empRespId) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_RESP_ID = ? ORDER BY EMP_RESP_ID", new Object[] {  new Integer(empRespId) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpResponsibilities[] findWhereEmployeeIdEquals(int employeeId) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ? ORDER BY EMPLOYEE_ID", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'RESPONSIBILITY_ID = :responsibilityId'.
	 */
	public EmpResponsibilities[] findWhereResponsibilityIdEquals(int responsibilityId) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE RESPONSIBILITY_ID = ? ORDER BY RESPONSIBILITY_ID", new Object[] {  new Integer(responsibilityId) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'RECEIVED_DATE = :receivedDate'.
	 */
	public EmpResponsibilities[] findWhereReceivedDateEquals(Date receivedDate) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE RECEIVED_DATE = ? ORDER BY RECEIVED_DATE", new Object[] { receivedDate==null ? null : new java.sql.Timestamp( receivedDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'DELIVERED_DATE = :deliveredDate'.
	 */
	public EmpResponsibilities[] findWhereDeliveredDateEquals(Date deliveredDate) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DELIVERED_DATE = ? ORDER BY DELIVERED_DATE", new Object[] { deliveredDate==null ? null : new java.sql.Timestamp( deliveredDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the criteria 'NOTE = :note'.
	 */
	public EmpResponsibilities[] findWhereNoteEquals(String note) throws EmpResponsibilitiesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOTE = ? ORDER BY NOTE", new Object[] { note } );
	}

	/**
	 * Method 'EmpResponsibilitiesDaoImpl'
	 * 
	 */
	public EmpResponsibilitiesDaoImpl()
	{
	}

	/**
	 * Method 'EmpResponsibilitiesDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpResponsibilitiesDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_RESPONSIBILITIES";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpResponsibilities fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpResponsibilities dto = new EmpResponsibilities();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpResponsibilities[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpResponsibilities dto = new EmpResponsibilities();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpResponsibilities ret[] = new EmpResponsibilities[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpResponsibilities dto, ResultSet rs) throws SQLException
	{
		dto.setEmpRespId( rs.getInt( COLUMN_EMP_RESP_ID ) );
		dto.setEmployeeId( rs.getInt( COLUMN_EMPLOYEE_ID ) );
		dto.setResponsibilityId( rs.getInt( COLUMN_RESPONSIBILITY_ID ) );
		dto.setReceivedDate( rs.getTimestamp(COLUMN_RECEIVED_DATE ) );
		dto.setDeliveredDate( rs.getTimestamp(COLUMN_DELIVERED_DATE ) );
		dto.setNote( rs.getString( COLUMN_NOTE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpResponsibilities dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the specified arbitrary SQL statement
	 */
	public EmpResponsibilities[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpResponsibilitiesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpResponsibilitiesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_RESPONSIBILITIES table that match the specified arbitrary SQL statement
	 */
	public EmpResponsibilities[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpResponsibilitiesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpResponsibilitiesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
