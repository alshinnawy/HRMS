/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import java.util.Date;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpProjectsDaoImpl extends AbstractDAO implements EmpProjectsDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_PROJECT_ID, EMPLOYEE_ID, PROJECT_ID, START_DATE, END_DATE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_PROJECT_ID, EMPLOYEE_ID, PROJECT_ID, START_DATE, END_DATE ) VALUES ( ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_PROJECT_ID = ?, EMPLOYEE_ID = ?, PROJECT_ID = ?, START_DATE = ?, END_DATE = ? WHERE EMP_PROJECT_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_PROJECT_ID = ?";

	/** 
	 * Index of column EMP_PROJECT_ID
	 */
	protected static final int COLUMN_EMP_PROJECT_ID = 1;

	/** 
	 * Index of column EMPLOYEE_ID
	 */
	protected static final int COLUMN_EMPLOYEE_ID = 2;

	/** 
	 * Index of column PROJECT_ID
	 */
	protected static final int COLUMN_PROJECT_ID = 3;

	/** 
	 * Index of column START_DATE
	 */
	protected static final int COLUMN_START_DATE = 4;

	/** 
	 * Index of column END_DATE
	 */
	protected static final int COLUMN_END_DATE = 5;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 5;

	/** 
	 * Index of primary-key column EMP_PROJECT_ID
	 */
	protected static final int PK_COLUMN_EMP_PROJECT_ID = 1;

	/** 
	 * Inserts a new row in the EMP_PROJECTS table.
	 */
	public EmpProjectsPk insert(EmpProjects dto) throws EmpProjectsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getEmpProjectId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getProjectId() );
			stmt.setTimestamp(index++, dto.getStartDate()==null ? null : new java.sql.Timestamp( dto.getStartDate().getTime() ) );
			stmt.setTimestamp(index++, dto.getEndDate()==null ? null : new java.sql.Timestamp( dto.getEndDate().getTime() ) );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpProjectsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_PROJECTS table.
	 */
	public void update(EmpProjectsPk pk, EmpProjects dto) throws EmpProjectsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getEmpProjectId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getProjectId() );
			stmt.setTimestamp(index++, dto.getStartDate()==null ? null : new java.sql.Timestamp( dto.getStartDate().getTime() ) );
			stmt.setTimestamp(index++, dto.getEndDate()==null ? null : new java.sql.Timestamp( dto.getEndDate().getTime() ) );
			stmt.setInt( 6, pk.getEmpProjectId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpProjectsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_PROJECTS table.
	 */
	public void delete(EmpProjectsPk pk) throws EmpProjectsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getEmpProjectId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpProjectsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_PROJECTS table that matches the specified primary-key value.
	 */
	public EmpProjects findByPrimaryKey(EmpProjectsPk pk) throws EmpProjectsDaoException
	{
		return findByPrimaryKey( pk.getEmpProjectId() );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'EMP_PROJECT_ID = :empProjectId'.
	 */
	public EmpProjects findByPrimaryKey(int empProjectId) throws EmpProjectsDaoException
	{
		EmpProjects ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_PROJECT_ID = ?", new Object[] {  new Integer(empProjectId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria ''.
	 */
	public EmpProjects[] findAll() throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_PROJECT_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpProjects[] findByEmployees(int employeeId) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ?", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'PROJECT_ID = :projectId'.
	 */
	public EmpProjects[] findByProjects(int projectId) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PROJECT_ID = ?", new Object[] {  new Integer(projectId) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'EMP_PROJECT_ID = :empProjectId'.
	 */
	public EmpProjects[] findWhereEmpProjectIdEquals(int empProjectId) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_PROJECT_ID = ? ORDER BY EMP_PROJECT_ID", new Object[] {  new Integer(empProjectId) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpProjects[] findWhereEmployeeIdEquals(int employeeId) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ? ORDER BY EMPLOYEE_ID", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'PROJECT_ID = :projectId'.
	 */
	public EmpProjects[] findWhereProjectIdEquals(int projectId) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PROJECT_ID = ? ORDER BY PROJECT_ID", new Object[] {  new Integer(projectId) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'START_DATE = :startDate'.
	 */
	public EmpProjects[] findWhereStartDateEquals(Date startDate) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE START_DATE = ? ORDER BY START_DATE", new Object[] { startDate==null ? null : new java.sql.Timestamp( startDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the criteria 'END_DATE = :endDate'.
	 */
	public EmpProjects[] findWhereEndDateEquals(Date endDate) throws EmpProjectsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE END_DATE = ? ORDER BY END_DATE", new Object[] { endDate==null ? null : new java.sql.Timestamp( endDate.getTime() ) } );
	}

	/**
	 * Method 'EmpProjectsDaoImpl'
	 * 
	 */
	public EmpProjectsDaoImpl()
	{
	}

	/**
	 * Method 'EmpProjectsDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpProjectsDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_PROJECTS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpProjects fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpProjects dto = new EmpProjects();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpProjects[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpProjects dto = new EmpProjects();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpProjects ret[] = new EmpProjects[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpProjects dto, ResultSet rs) throws SQLException
	{
		dto.setEmpProjectId( rs.getInt( COLUMN_EMP_PROJECT_ID ) );
		dto.setEmployeeId( rs.getInt( COLUMN_EMPLOYEE_ID ) );
		dto.setProjectId( rs.getInt( COLUMN_PROJECT_ID ) );
		dto.setStartDate( rs.getTimestamp(COLUMN_START_DATE ) );
		dto.setEndDate( rs.getTimestamp(COLUMN_END_DATE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpProjects dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the specified arbitrary SQL statement
	 */
	public EmpProjects[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpProjectsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpProjectsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_PROJECTS table that match the specified arbitrary SQL statement
	 */
	public EmpProjects[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpProjectsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpProjectsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
