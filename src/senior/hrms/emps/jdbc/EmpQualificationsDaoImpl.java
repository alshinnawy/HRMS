/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpQualificationsDaoImpl extends AbstractDAO implements EmpQualificationsDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_QUAL_ID, EMPLOYEE_ID, INSTITUTE_ID, DEGREE_ID, MAJOR_ID, GRADE_ID, GRADUATION_YEAR FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_QUAL_ID, EMPLOYEE_ID, INSTITUTE_ID, DEGREE_ID, MAJOR_ID, GRADE_ID, GRADUATION_YEAR ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_QUAL_ID = ?, EMPLOYEE_ID = ?, INSTITUTE_ID = ?, DEGREE_ID = ?, MAJOR_ID = ?, GRADE_ID = ?, GRADUATION_YEAR = ? WHERE EMP_QUAL_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_QUAL_ID = ?";

	/** 
	 * Index of column EMP_QUAL_ID
	 */
	protected static final int COLUMN_EMP_QUAL_ID = 1;

	/** 
	 * Index of column EMPLOYEE_ID
	 */
	protected static final int COLUMN_EMPLOYEE_ID = 2;

	/** 
	 * Index of column INSTITUTE_ID
	 */
	protected static final int COLUMN_INSTITUTE_ID = 3;

	/** 
	 * Index of column DEGREE_ID
	 */
	protected static final int COLUMN_DEGREE_ID = 4;

	/** 
	 * Index of column MAJOR_ID
	 */
	protected static final int COLUMN_MAJOR_ID = 5;

	/** 
	 * Index of column GRADE_ID
	 */
	protected static final int COLUMN_GRADE_ID = 6;

	/** 
	 * Index of column GRADUATION_YEAR
	 */
	protected static final int COLUMN_GRADUATION_YEAR = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column EMP_QUAL_ID
	 */
	protected static final int PK_COLUMN_EMP_QUAL_ID = 1;

	/** 
	 * Inserts a new row in the EMP_QUALIFICATIONS table.
	 */
	public EmpQualificationsPk insert(EmpQualifications dto) throws EmpQualificationsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getEmpQualId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getInstituteId() );
			stmt.setInt( index++, dto.getDegreeId() );
			if (dto.isMajorIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getMajorId() );
			}
		
			stmt.setInt( index++, dto.getGradeId() );
			stmt.setString( index++, dto.getGraduationYear() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpQualificationsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_QUALIFICATIONS table.
	 */
	public void update(EmpQualificationsPk pk, EmpQualifications dto) throws EmpQualificationsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getEmpQualId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getInstituteId() );
			stmt.setInt( index++, dto.getDegreeId() );
			if (dto.isMajorIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getMajorId() );
			}
		
			stmt.setInt( index++, dto.getGradeId() );
			stmt.setString( index++, dto.getGraduationYear() );
			stmt.setInt( 8, pk.getEmpQualId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpQualificationsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_QUALIFICATIONS table.
	 */
	public void delete(EmpQualificationsPk pk) throws EmpQualificationsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getEmpQualId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpQualificationsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_QUALIFICATIONS table that matches the specified primary-key value.
	 */
	public EmpQualifications findByPrimaryKey(EmpQualificationsPk pk) throws EmpQualificationsDaoException
	{
		return findByPrimaryKey( pk.getEmpQualId() );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'EMP_QUAL_ID = :empQualId'.
	 */
	public EmpQualifications findByPrimaryKey(int empQualId) throws EmpQualificationsDaoException
	{
		EmpQualifications ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_QUAL_ID = ?", new Object[] {  new Integer(empQualId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria ''.
	 */
	public EmpQualifications[] findAll() throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_QUAL_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'DEGREE_ID = :degreeId'.
	 */
	public EmpQualifications[] findByDegree(int degreeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DEGREE_ID = ?", new Object[] {  new Integer(degreeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpQualifications[] findByEmployees(int employeeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ?", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'GRADE_ID = :gradeId'.
	 */
	public EmpQualifications[] findByGrade(int gradeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE GRADE_ID = ?", new Object[] {  new Integer(gradeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'INSTITUTE_ID = :instituteId'.
	 */
	public EmpQualifications[] findByInstitutions(int instituteId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE INSTITUTE_ID = ?", new Object[] {  new Integer(instituteId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'MAJOR_ID = :majorId'.
	 */
	public EmpQualifications[] findByMajor(int majorId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE MAJOR_ID = ?", new Object[] {  new Integer(majorId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'EMP_QUAL_ID = :empQualId'.
	 */
	public EmpQualifications[] findWhereEmpQualIdEquals(int empQualId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_QUAL_ID = ? ORDER BY EMP_QUAL_ID", new Object[] {  new Integer(empQualId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpQualifications[] findWhereEmployeeIdEquals(int employeeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ? ORDER BY EMPLOYEE_ID", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'INSTITUTE_ID = :instituteId'.
	 */
	public EmpQualifications[] findWhereInstituteIdEquals(int instituteId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE INSTITUTE_ID = ? ORDER BY INSTITUTE_ID", new Object[] {  new Integer(instituteId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'DEGREE_ID = :degreeId'.
	 */
	public EmpQualifications[] findWhereDegreeIdEquals(int degreeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DEGREE_ID = ? ORDER BY DEGREE_ID", new Object[] {  new Integer(degreeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'MAJOR_ID = :majorId'.
	 */
	public EmpQualifications[] findWhereMajorIdEquals(int majorId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE MAJOR_ID = ? ORDER BY MAJOR_ID", new Object[] {  new Integer(majorId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'GRADE_ID = :gradeId'.
	 */
	public EmpQualifications[] findWhereGradeIdEquals(int gradeId) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE GRADE_ID = ? ORDER BY GRADE_ID", new Object[] {  new Integer(gradeId) } );
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the criteria 'GRADUATION_YEAR = :graduationYear'.
	 */
	public EmpQualifications[] findWhereGraduationYearEquals(String graduationYear) throws EmpQualificationsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE GRADUATION_YEAR = ? ORDER BY GRADUATION_YEAR", new Object[] { graduationYear } );
	}

	/**
	 * Method 'EmpQualificationsDaoImpl'
	 * 
	 */
	public EmpQualificationsDaoImpl()
	{
	}

	/**
	 * Method 'EmpQualificationsDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpQualificationsDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_QUALIFICATIONS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpQualifications fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpQualifications dto = new EmpQualifications();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpQualifications[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpQualifications dto = new EmpQualifications();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpQualifications ret[] = new EmpQualifications[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpQualifications dto, ResultSet rs) throws SQLException
	{
		dto.setEmpQualId( rs.getInt( COLUMN_EMP_QUAL_ID ) );
		dto.setEmployeeId( rs.getInt( COLUMN_EMPLOYEE_ID ) );
		dto.setInstituteId( rs.getInt( COLUMN_INSTITUTE_ID ) );
		dto.setDegreeId( rs.getInt( COLUMN_DEGREE_ID ) );
		dto.setMajorId( rs.getInt( COLUMN_MAJOR_ID ) );
		if (rs.wasNull()) {
			dto.setMajorIdNull( true );
		}
		
		dto.setGradeId( rs.getInt( COLUMN_GRADE_ID ) );
		dto.setGraduationYear( rs.getString( COLUMN_GRADUATION_YEAR ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpQualifications dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the specified arbitrary SQL statement
	 */
	public EmpQualifications[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpQualificationsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpQualificationsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_QUALIFICATIONS table that match the specified arbitrary SQL statement
	 */
	public EmpQualifications[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpQualificationsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpQualificationsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
