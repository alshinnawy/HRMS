/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import java.util.Date;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpAbsenceDaoImpl extends AbstractDAO implements EmpAbsenceDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_ABSENCE_ID, EMPLOYEE_ID, ABSENCE_DATE, NOTE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_ABSENCE_ID, EMPLOYEE_ID, ABSENCE_DATE, NOTE ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_ABSENCE_ID = ?, EMPLOYEE_ID = ?, ABSENCE_DATE = ?, NOTE = ? WHERE EMP_ABSENCE_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_ABSENCE_ID = ?";

	/** 
	 * Index of column EMP_ABSENCE_ID
	 */
	protected static final int COLUMN_EMP_ABSENCE_ID = 1;

	/** 
	 * Index of column EMPLOYEE_ID
	 */
	protected static final int COLUMN_EMPLOYEE_ID = 2;

	/** 
	 * Index of column ABSENCE_DATE
	 */
	protected static final int COLUMN_ABSENCE_DATE = 3;

	/** 
	 * Index of column NOTE
	 */
	protected static final int COLUMN_NOTE = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column EMP_ABSENCE_ID
	 */
	protected static final int PK_COLUMN_EMP_ABSENCE_ID = 1;

	/** 
	 * Inserts a new row in the EMP_ABSENCE table.
	 */
	public EmpAbsencePk insert(EmpAbsence dto) throws EmpAbsenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getEmpAbsenceId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setTimestamp(index++, dto.getAbsenceDate()==null ? null : new java.sql.Timestamp( dto.getAbsenceDate().getTime() ) );
			stmt.setString( index++, dto.getNote() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpAbsenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_ABSENCE table.
	 */
	public void update(EmpAbsencePk pk, EmpAbsence dto) throws EmpAbsenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getEmpAbsenceId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setTimestamp(index++, dto.getAbsenceDate()==null ? null : new java.sql.Timestamp( dto.getAbsenceDate().getTime() ) );
			stmt.setString( index++, dto.getNote() );
			stmt.setLong( 5, pk.getEmpAbsenceId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpAbsenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_ABSENCE table.
	 */
	public void delete(EmpAbsencePk pk) throws EmpAbsenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getEmpAbsenceId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpAbsenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_ABSENCE table that matches the specified primary-key value.
	 */
	public EmpAbsence findByPrimaryKey(EmpAbsencePk pk) throws EmpAbsenceDaoException
	{
		return findByPrimaryKey( pk.getEmpAbsenceId() );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'EMP_ABSENCE_ID = :empAbsenceId'.
	 */
	public EmpAbsence findByPrimaryKey(long empAbsenceId) throws EmpAbsenceDaoException
	{
		EmpAbsence ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_ABSENCE_ID = ?", new Object[] {  new Long(empAbsenceId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria ''.
	 */
	public EmpAbsence[] findAll() throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_ABSENCE_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpAbsence[] findByEmployees(int employeeId) throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ?", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'EMP_ABSENCE_ID = :empAbsenceId'.
	 */
	public EmpAbsence[] findWhereEmpAbsenceIdEquals(long empAbsenceId) throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_ABSENCE_ID = ? ORDER BY EMP_ABSENCE_ID", new Object[] {  new Long(empAbsenceId) } );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpAbsence[] findWhereEmployeeIdEquals(int employeeId) throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ? ORDER BY EMPLOYEE_ID", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'ABSENCE_DATE = :absenceDate'.
	 */
	public EmpAbsence[] findWhereAbsenceDateEquals(Date absenceDate) throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ABSENCE_DATE = ? ORDER BY ABSENCE_DATE", new Object[] { absenceDate==null ? null : new java.sql.Timestamp( absenceDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the criteria 'NOTE = :note'.
	 */
	public EmpAbsence[] findWhereNoteEquals(String note) throws EmpAbsenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOTE = ? ORDER BY NOTE", new Object[] { note } );
	}

	/**
	 * Method 'EmpAbsenceDaoImpl'
	 * 
	 */
	public EmpAbsenceDaoImpl()
	{
	}

	/**
	 * Method 'EmpAbsenceDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpAbsenceDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_ABSENCE";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpAbsence fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpAbsence dto = new EmpAbsence();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpAbsence[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpAbsence dto = new EmpAbsence();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpAbsence ret[] = new EmpAbsence[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpAbsence dto, ResultSet rs) throws SQLException
	{
		dto.setEmpAbsenceId( rs.getLong( COLUMN_EMP_ABSENCE_ID ) );
		dto.setEmployeeId( rs.getInt( COLUMN_EMPLOYEE_ID ) );
		dto.setAbsenceDate( rs.getTimestamp(COLUMN_ABSENCE_DATE ) );
		dto.setNote( rs.getString( COLUMN_NOTE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpAbsence dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the specified arbitrary SQL statement
	 */
	public EmpAbsence[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpAbsenceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpAbsenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_ABSENCE table that match the specified arbitrary SQL statement
	 */
	public EmpAbsence[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpAbsenceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpAbsenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
