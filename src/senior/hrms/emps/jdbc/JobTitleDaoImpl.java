/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class JobTitleDaoImpl extends AbstractDAO implements JobTitleDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT JOB_TITLE_ID, JOB_TITLE_NAME, JOB_DESCRIPTION FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( JOB_TITLE_ID, JOB_TITLE_NAME, JOB_DESCRIPTION ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET JOB_TITLE_ID = ?, JOB_TITLE_NAME = ?, JOB_DESCRIPTION = ? WHERE JOB_TITLE_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE JOB_TITLE_ID = ?";

	/** 
	 * Index of column JOB_TITLE_ID
	 */
	protected static final int COLUMN_JOB_TITLE_ID = 1;

	/** 
	 * Index of column JOB_TITLE_NAME
	 */
	protected static final int COLUMN_JOB_TITLE_NAME = 2;

	/** 
	 * Index of column JOB_DESCRIPTION
	 */
	protected static final int COLUMN_JOB_DESCRIPTION = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column JOB_TITLE_ID
	 */
	protected static final int PK_COLUMN_JOB_TITLE_ID = 1;

	/** 
	 * Inserts a new row in the JOB_TITLE table.
	 */
	public JobTitlePk insert(JobTitle dto) throws JobTitleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getJobTitleId() );
			stmt.setString( index++, dto.getJobTitleName() );
			stmt.setString( index++, dto.getJobDescription() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new JobTitleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the JOB_TITLE table.
	 */
	public void update(JobTitlePk pk, JobTitle dto) throws JobTitleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getJobTitleId() );
			stmt.setString( index++, dto.getJobTitleName() );
			stmt.setString( index++, dto.getJobDescription() );
			stmt.setInt( 4, pk.getJobTitleId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new JobTitleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the JOB_TITLE table.
	 */
	public void delete(JobTitlePk pk) throws JobTitleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getJobTitleId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new JobTitleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the JOB_TITLE table that matches the specified primary-key value.
	 */
	public JobTitle findByPrimaryKey(JobTitlePk pk) throws JobTitleDaoException
	{
		return findByPrimaryKey( pk.getJobTitleId() );
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the criteria 'JOB_TITLE_ID = :jobTitleId'.
	 */
	public JobTitle findByPrimaryKey(int jobTitleId) throws JobTitleDaoException
	{
		JobTitle ret[] = findByDynamicSelect( SQL_SELECT + " WHERE JOB_TITLE_ID = ?", new Object[] {  new Integer(jobTitleId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the criteria ''.
	 */
	public JobTitle[] findAll() throws JobTitleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY JOB_TITLE_ID", null );
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the criteria 'JOB_TITLE_ID = :jobTitleId'.
	 */
	public JobTitle[] findWhereJobTitleIdEquals(int jobTitleId) throws JobTitleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE JOB_TITLE_ID = ? ORDER BY JOB_TITLE_ID", new Object[] {  new Integer(jobTitleId) } );
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the criteria 'JOB_TITLE_NAME = :jobTitleName'.
	 */
	public JobTitle[] findWhereJobTitleNameEquals(String jobTitleName) throws JobTitleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE JOB_TITLE_NAME = ? ORDER BY JOB_TITLE_NAME", new Object[] { jobTitleName } );
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the criteria 'JOB_DESCRIPTION = :jobDescription'.
	 */
	public JobTitle[] findWhereJobDescriptionEquals(String jobDescription) throws JobTitleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE JOB_DESCRIPTION = ? ORDER BY JOB_DESCRIPTION", new Object[] { jobDescription } );
	}

	/**
	 * Method 'JobTitleDaoImpl'
	 * 
	 */
	public JobTitleDaoImpl()
	{
	}

	/**
	 * Method 'JobTitleDaoImpl'
	 * 
	 * @param userConn
	 */
	public JobTitleDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.JOB_TITLE";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected JobTitle fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			JobTitle dto = new JobTitle();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected JobTitle[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			JobTitle dto = new JobTitle();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		JobTitle ret[] = new JobTitle[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(JobTitle dto, ResultSet rs) throws SQLException
	{
		dto.setJobTitleId( rs.getInt( COLUMN_JOB_TITLE_ID ) );
		dto.setJobTitleName( rs.getString( COLUMN_JOB_TITLE_NAME ) );
		dto.setJobDescription( rs.getString( COLUMN_JOB_DESCRIPTION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(JobTitle dto)
	{
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the specified arbitrary SQL statement
	 */
	public JobTitle[] findByDynamicSelect(String sql, Object[] sqlParams) throws JobTitleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new JobTitleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the JOB_TITLE table that match the specified arbitrary SQL statement
	 */
	public JobTitle[] findByDynamicWhere(String sql, Object[] sqlParams) throws JobTitleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new JobTitleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
