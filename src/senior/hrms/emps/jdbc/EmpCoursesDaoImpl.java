/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpCoursesDaoImpl extends AbstractDAO implements EmpCoursesDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_COURSE_ID, EMPLOYEE_ID, COURSE_ID FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_COURSE_ID, EMPLOYEE_ID, COURSE_ID ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_COURSE_ID = ?, EMPLOYEE_ID = ?, COURSE_ID = ? WHERE EMP_COURSE_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_COURSE_ID = ?";

	/** 
	 * Index of column EMP_COURSE_ID
	 */
	protected static final int COLUMN_EMP_COURSE_ID = 1;

	/** 
	 * Index of column EMPLOYEE_ID
	 */
	protected static final int COLUMN_EMPLOYEE_ID = 2;

	/** 
	 * Index of column COURSE_ID
	 */
	protected static final int COLUMN_COURSE_ID = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column EMP_COURSE_ID
	 */
	protected static final int PK_COLUMN_EMP_COURSE_ID = 1;

	/** 
	 * Inserts a new row in the EMP_COURSES table.
	 */
	public EmpCoursesPk insert(EmpCourses dto) throws EmpCoursesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getEmpCourseId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getCourseId() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpCoursesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_COURSES table.
	 */
	public void update(EmpCoursesPk pk, EmpCourses dto) throws EmpCoursesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getEmpCourseId() );
			stmt.setInt( index++, dto.getEmployeeId() );
			stmt.setInt( index++, dto.getCourseId() );
			stmt.setInt( 4, pk.getEmpCourseId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpCoursesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_COURSES table.
	 */
	public void delete(EmpCoursesPk pk) throws EmpCoursesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getEmpCourseId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpCoursesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_COURSES table that matches the specified primary-key value.
	 */
	public EmpCourses findByPrimaryKey(EmpCoursesPk pk) throws EmpCoursesDaoException
	{
		return findByPrimaryKey( pk.getEmpCourseId() );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'EMP_COURSE_ID = :empCourseId'.
	 */
	public EmpCourses findByPrimaryKey(int empCourseId) throws EmpCoursesDaoException
	{
		EmpCourses ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_COURSE_ID = ?", new Object[] {  new Integer(empCourseId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria ''.
	 */
	public EmpCourses[] findAll() throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_COURSE_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'COURSE_ID = :courseId'.
	 */
	public EmpCourses[] findByCourses(int courseId) throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE COURSE_ID = ?", new Object[] {  new Integer(courseId) } );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpCourses[] findByEmployees(int employeeId) throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ?", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'EMP_COURSE_ID = :empCourseId'.
	 */
	public EmpCourses[] findWhereEmpCourseIdEquals(int empCourseId) throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_COURSE_ID = ? ORDER BY EMP_COURSE_ID", new Object[] {  new Integer(empCourseId) } );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'EMPLOYEE_ID = :employeeId'.
	 */
	public EmpCourses[] findWhereEmployeeIdEquals(int employeeId) throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMPLOYEE_ID = ? ORDER BY EMPLOYEE_ID", new Object[] {  new Integer(employeeId) } );
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the criteria 'COURSE_ID = :courseId'.
	 */
	public EmpCourses[] findWhereCourseIdEquals(int courseId) throws EmpCoursesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE COURSE_ID = ? ORDER BY COURSE_ID", new Object[] {  new Integer(courseId) } );
	}

	/**
	 * Method 'EmpCoursesDaoImpl'
	 * 
	 */
	public EmpCoursesDaoImpl()
	{
	}

	/**
	 * Method 'EmpCoursesDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpCoursesDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_COURSES";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpCourses fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpCourses dto = new EmpCourses();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpCourses[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpCourses dto = new EmpCourses();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpCourses ret[] = new EmpCourses[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpCourses dto, ResultSet rs) throws SQLException
	{
		dto.setEmpCourseId( rs.getInt( COLUMN_EMP_COURSE_ID ) );
		dto.setEmployeeId( rs.getInt( COLUMN_EMPLOYEE_ID ) );
		dto.setCourseId( rs.getInt( COLUMN_COURSE_ID ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpCourses dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the specified arbitrary SQL statement
	 */
	public EmpCourses[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpCoursesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpCoursesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_COURSES table that match the specified arbitrary SQL statement
	 */
	public EmpCourses[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpCoursesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpCoursesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
