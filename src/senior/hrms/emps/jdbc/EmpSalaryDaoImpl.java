/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package senior.hrms.emps.jdbc;

import senior.hrms.emps.dao.*;
import senior.hrms.emps.factory.*;
import java.math.BigDecimal;
import senior.hrms.emps.dto.*;
import senior.hrms.emps.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmpSalaryDaoImpl extends AbstractDAO implements EmpSalaryDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT EMP_JOB_ID, FINANCIAL_YEAR_ID, PRICE_PER_HOUR, BASIC_SALARY, PROFIT_SHARE, RAISE_RATIO, TAX_RATIO FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( EMP_JOB_ID, FINANCIAL_YEAR_ID, PRICE_PER_HOUR, BASIC_SALARY, PROFIT_SHARE, RAISE_RATIO, TAX_RATIO ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET EMP_JOB_ID = ?, FINANCIAL_YEAR_ID = ?, PRICE_PER_HOUR = ?, BASIC_SALARY = ?, PROFIT_SHARE = ?, RAISE_RATIO = ?, TAX_RATIO = ? WHERE EMP_JOB_ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE EMP_JOB_ID = ?";

	/** 
	 * Index of column EMP_JOB_ID
	 */
	protected static final int COLUMN_EMP_JOB_ID = 1;

	/** 
	 * Index of column FINANCIAL_YEAR_ID
	 */
	protected static final int COLUMN_FINANCIAL_YEAR_ID = 2;

	/** 
	 * Index of column PRICE_PER_HOUR
	 */
	protected static final int COLUMN_PRICE_PER_HOUR = 3;

	/** 
	 * Index of column BASIC_SALARY
	 */
	protected static final int COLUMN_BASIC_SALARY = 4;

	/** 
	 * Index of column PROFIT_SHARE
	 */
	protected static final int COLUMN_PROFIT_SHARE = 5;

	/** 
	 * Index of column RAISE_RATIO
	 */
	protected static final int COLUMN_RAISE_RATIO = 6;

	/** 
	 * Index of column TAX_RATIO
	 */
	protected static final int COLUMN_TAX_RATIO = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column EMP_JOB_ID
	 */
	protected static final int PK_COLUMN_EMP_JOB_ID = 1;

	/** 
	 * Inserts a new row in the EMP_SALARY table.
	 */
	public EmpSalaryPk insert(EmpSalary dto) throws EmpSalaryDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getEmpJobId() );
			stmt.setInt( index++, dto.getFinancialYearId() );
			stmt.setBigDecimal( index++, dto.getPricePerHour() );
			stmt.setBigDecimal( index++, dto.getBasicSalary() );
			stmt.setBigDecimal( index++, dto.getProfitShare() );
			stmt.setBigDecimal( index++, dto.getRaiseRatio() );
			stmt.setBigDecimal( index++, dto.getTaxRatio() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpSalaryDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the EMP_SALARY table.
	 */
	public void update(EmpSalaryPk pk, EmpSalary dto) throws EmpSalaryDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getEmpJobId() );
			stmt.setInt( index++, dto.getFinancialYearId() );
			stmt.setBigDecimal( index++, dto.getPricePerHour() );
			stmt.setBigDecimal( index++, dto.getBasicSalary() );
			stmt.setBigDecimal( index++, dto.getProfitShare() );
			stmt.setBigDecimal( index++, dto.getRaiseRatio() );
			stmt.setBigDecimal( index++, dto.getTaxRatio() );
			stmt.setInt( 8, pk.getEmpJobId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpSalaryDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the EMP_SALARY table.
	 */
	public void delete(EmpSalaryPk pk) throws EmpSalaryDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getEmpJobId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpSalaryDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the EMP_SALARY table that matches the specified primary-key value.
	 */
	public EmpSalary findByPrimaryKey(EmpSalaryPk pk) throws EmpSalaryDaoException
	{
		return findByPrimaryKey( pk.getEmpJobId() );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'EMP_JOB_ID = :empJobId'.
	 */
	public EmpSalary findByPrimaryKey(int empJobId) throws EmpSalaryDaoException
	{
		EmpSalary ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EMP_JOB_ID = ?", new Object[] {  new Integer(empJobId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria ''.
	 */
	public EmpSalary[] findAll() throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY EMP_JOB_ID", null );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'EMP_JOB_ID = :empJobId'.
	 */
	public EmpSalary[] findByEmployeeJobs(int empJobId) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_JOB_ID = ?", new Object[] {  new Integer(empJobId) } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'FINANCIAL_YEAR_ID = :financialYearId'.
	 */
	public EmpSalary[] findByFinancialYears(int financialYearId) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FINANCIAL_YEAR_ID = ?", new Object[] {  new Integer(financialYearId) } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'EMP_JOB_ID = :empJobId'.
	 */
	public EmpSalary[] findWhereEmpJobIdEquals(int empJobId) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EMP_JOB_ID = ? ORDER BY EMP_JOB_ID", new Object[] {  new Integer(empJobId) } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'FINANCIAL_YEAR_ID = :financialYearId'.
	 */
	public EmpSalary[] findWhereFinancialYearIdEquals(int financialYearId) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FINANCIAL_YEAR_ID = ? ORDER BY FINANCIAL_YEAR_ID", new Object[] {  new Integer(financialYearId) } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'PRICE_PER_HOUR = :pricePerHour'.
	 */
	public EmpSalary[] findWherePricePerHourEquals(BigDecimal pricePerHour) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PRICE_PER_HOUR = ? ORDER BY PRICE_PER_HOUR", new Object[] { pricePerHour } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'BASIC_SALARY = :basicSalary'.
	 */
	public EmpSalary[] findWhereBasicSalaryEquals(BigDecimal basicSalary) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE BASIC_SALARY = ? ORDER BY BASIC_SALARY", new Object[] { basicSalary } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'PROFIT_SHARE = :profitShare'.
	 */
	public EmpSalary[] findWhereProfitShareEquals(BigDecimal profitShare) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PROFIT_SHARE = ? ORDER BY PROFIT_SHARE", new Object[] { profitShare } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'RAISE_RATIO = :raiseRatio'.
	 */
	public EmpSalary[] findWhereRaiseRatioEquals(BigDecimal raiseRatio) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE RAISE_RATIO = ? ORDER BY RAISE_RATIO", new Object[] { raiseRatio } );
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the criteria 'TAX_RATIO = :taxRatio'.
	 */
	public EmpSalary[] findWhereTaxRatioEquals(BigDecimal taxRatio) throws EmpSalaryDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TAX_RATIO = ? ORDER BY TAX_RATIO", new Object[] { taxRatio } );
	}

	/**
	 * Method 'EmpSalaryDaoImpl'
	 * 
	 */
	public EmpSalaryDaoImpl()
	{
	}

	/**
	 * Method 'EmpSalaryDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmpSalaryDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "HRMS.EMP_SALARY";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmpSalary fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmpSalary dto = new EmpSalary();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmpSalary[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmpSalary dto = new EmpSalary();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmpSalary ret[] = new EmpSalary[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmpSalary dto, ResultSet rs) throws SQLException
	{
		dto.setEmpJobId( rs.getInt( COLUMN_EMP_JOB_ID ) );
		dto.setFinancialYearId( rs.getInt( COLUMN_FINANCIAL_YEAR_ID ) );
		dto.setPricePerHour( rs.getBigDecimal(COLUMN_PRICE_PER_HOUR));
		dto.setBasicSalary( rs.getBigDecimal(COLUMN_BASIC_SALARY));
		dto.setProfitShare( rs.getBigDecimal(COLUMN_PROFIT_SHARE));
		dto.setRaiseRatio( rs.getBigDecimal(COLUMN_RAISE_RATIO));
		dto.setTaxRatio( rs.getBigDecimal(COLUMN_TAX_RATIO));
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmpSalary dto)
	{
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the specified arbitrary SQL statement
	 */
	public EmpSalary[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmpSalaryDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpSalaryDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the EMP_SALARY table that match the specified arbitrary SQL statement
	 */
	public EmpSalary[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmpSalaryDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmpSalaryDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
